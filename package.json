{
  "name": "premium-website",
  "version": "1.0.0",
  "description": "Premium interactive portfolio website",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html --public-url ./ --dist-dir docs",
    "clean": "rm -rf docs/ .parcel-cache/",
    "serve": "parcel serve src/index.html"
  },
  "devDependencies": {
    "parcel": "^2.8.3",
    "svgo": "^3.3.2",
    "typescript": "^4.9.5"
  }
}
